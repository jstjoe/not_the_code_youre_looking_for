
var zendeskCore = require('../lib/client').createClient({
  username: process.env.ZENDESK_USERNAME,
  token: process.env.ZENDESK_TOKEN,
  remoteUri: 'https://' + process.env.ZENDESK_SUBDOMAIN + '.zendesk.com/api/v2/'
});

zendeskCore.macros.categories(function (err, req, result) {
  if (err) {
    console.log(err);
  }
  console.log(result);
});